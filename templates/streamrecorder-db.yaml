apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.applications.db.name }}
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/henesi-development/StreamRecorder-Helm-Charts
    targetRevision: master
    path: {{ .Values.applications.db.repoPath }}
  destination:
    name: {{ .Values.applications.cluster.name }}
    namespace: {{ .Values.applications.db.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true