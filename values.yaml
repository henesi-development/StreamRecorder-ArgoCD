applications:
  api:
    name: streamrecorder-api
    namespace: stream-recorder
    repoPath: streamrecorder-api

  postgresql:
    name: streamrecorder-postgresql
    namespace: stream-recorder
    repoPath: streamrecorder-postgresql
  
  network:
    name: streamrecorder-network
    namespace: stream-recorder
    repoPath: streamrecorder-network

  cluster:
    server: https://kubernetes.default.svc